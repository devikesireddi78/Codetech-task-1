    #include <fmt/core.h>
    #include <string>

    int main() {
        int value = 42;
        std::string message = fmt::format("The answer is {}.", value);
        // message will be "The answer is 42."
        return 0;
    }
